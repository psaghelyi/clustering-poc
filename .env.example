# AWS Configuration
AWS_REGION=us-east-1

# Embedding Model Provider (nova | titan | cohere)
EMBEDDING_PROVIDER=titan

# Claude Model ID for document merging
CLAUDE_MODEL_ID=anthropic.claude-haiku-4-5-20251001-v1:0

# Vector Storage Backend (simple-s3 | s3-vectors)
VECTOR_STORE_TYPE=simple-s3

# Simple S3 Configuration (for VECTOR_STORE_TYPE=simple-s3)
S3_BUCKET=clustering-poc-embeddings

# S3 Vectors Configuration (for VECTOR_STORE_TYPE=s3-vectors)
# Note: S3 Vectors is in preview and requires real AWS credentials
# Available regions: us-east-1, us-east-2, us-west-2, eu-central-1, ap-southeast-2
S3_VECTOR_BUCKET=clustering-poc-vectors
S3_VECTOR_INDEX=embeddings-index
EMBEDDING_DIMENSIONS=1024

# Testing Options
USE_MOCK_EMBEDDINGS=true

# Note: AWS credentials are handled by aws-vault
# Run with: aws-vault exec <profile> -- npm run merge
# Or: aws-vault exec <profile> -- npm run dev
