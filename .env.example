# AWS Configuration
AWS_ACCOUNT=acl-playground
AWS_REGION=us-east-1

# Embedding Model Provider (nova | titan | cohere)
EMBEDDING_PROVIDER=titan

# LLM Model for Document Merging (claude-haiku | nova)
LLM_MODEL=claude-haiku

# Advanced: Override specific model IDs (optional)
# CLAUDE_MODEL_ID=us.anthropic.claude-haiku-4-5-20251001-v1:0
# NOVA_MODEL_ID=us.amazon.nova-lite-v1:0

# S3 Vectors Configuration
# Note: S3 Vectors is in preview and requires real AWS credentials
# Available regions: us-east-1, us-east-2, us-west-2, eu-central-1, ap-southeast-2
S3_VECTOR_BUCKET=clustering-poc-vectors
S3_VECTOR_INDEX=embeddings-index

# Embedding dimensions (must match provider capabilities)
# - Titan: up to 1024 (default: 1024)
# - Nova: 256, 384, 512, 1024 (default: 1024)
# - Cohere v4 (Matryoshka): 256, 512, 1024, 1536 (default: 1536)
# Note: Changing dimensions requires recreating the S3 Vectors index
EMBEDDING_DIMENSIONS=1024

# HDBSCAN Clustering Configuration
# Distance metric for similarity calculation (euclidean | cosine)
#   - euclidean: Standard Euclidean distance (default, faster)
#   - cosine: Cosine similarity (recommended for text embeddings)
DISTANCE_METRIC=euclidean

# Clustering parameters
# minClusterSize: Minimum documents to form a cluster (default: 2)
#   - Lower (2-3): More, smaller clusters
#   - Higher (5-10): Fewer, larger clusters
# minSamples: Minimum samples for core points (default: 2)
#   - Lower: More sensitive to local density variations
#   - Higher: More robust to noise
MIN_CLUSTER_SIZE=2
MIN_SAMPLES=2

# Note: AWS credentials are handled by aws-vault
# Run with: aws-vault exec <profile> -- npm run merge:aws
